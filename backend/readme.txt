la lógica se divide en tres archivos diferentes: index.ts, typeDefs.ts y resolvers.ts. 
El archivo index.ts contiene el código que inicia el servidor, 
typeDefs.ts contiene la definición de los tipos GraphQL 
y el archivo resolvers.ts contiene la implementación de las operaciones de consulta y mutación.